[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rifthub-backend"
version = "0.1.0"
description = "Shared backend package for RiftHub runtime services."
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/rifthub_backend"]
